'use strict';

//import {Kitten} from '../db/mongoseDB.js'


let getAllBooks =  async function(req, result) {
  // const kittens  = Kitten.find();
 //  result(null, kittens);
};

let createBook =  async function(bookToCreate, result) {

   // const user = new Kitten(bookToCreate);
   // await user.save();
  
  //  result(null, bookToCreate);
};

export { getAllBooks , createBook};